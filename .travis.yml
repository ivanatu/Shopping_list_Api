language: python
python:
  - "3.6.1"

install:
  - pip install -r requirements.txt

services:
  - postgresql

before_script:
  - psql -c "create database shoplist;" -U postgres

# command to run tests
script:
  - nosetests --with-coverage test_app.py

after_success:
  - coveralls